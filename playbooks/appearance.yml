---
- name: Apply appearance profile
  hosts: all
  gather_facts: true

  pre_tasks:
    - name: Validate deployment inputs
      ansible.builtin.include_tasks: tasks/validate.yml

  roles:
    - fonts
    - config
    - terminal
    - shell
    - git
