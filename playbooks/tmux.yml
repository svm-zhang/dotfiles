---
- name: Manage tmux configuration
  hosts: all
  gather_facts: true

  pre_tasks:
    - name: Validate deployment inputs
      ansible.builtin.include_tasks: tasks/validate.yml

  roles:
    - tmux
